# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# Editor / agent local state
.claude/
.idea/
.vscode/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do NOT commit)
.env*
!.env.example

# typescript
*.tsbuildinfo

# leads (local file-store; rotate to a real DB in prod)
/data/leads.json

# Generated chandelier imagery is NOW tracked so deploys ship with the photos.
# (The Leonardo regeneration is expensive enough that we don't want to re-run
# it in CI.) If you need to keep heavy binaries out of git later, switch to
# Git LFS or a CDN bucket.
# /public/assets/products/generated/

# Local-only build artefact
tsconfig.tsbuildinfo

# Alternates / archives kept locally for review but never deployed
/public/assets/products/_alternates/
/public/assets/products/_archive/
